namespace TodoList.Domain.Enums;

/// <summary>
/// Represents the recurrence interval for repeating tasks.
/// </summary>
public enum RecurrenceInterval
{
    None = 0,
    Daily = 1,
    Weekly = 2,
    Monthly = 3,
    Yearly = 4
}
